#include <stdio.h>
/**
 * main - Entry
 * followed by a new line
 * Return: Always 0
 */
int main(void)
{

	char low;

	for (low = 'a'; low <= 'z'; low++)
		putchar(low);
	{

		putchar('\n');

	}

	return (0);
}
