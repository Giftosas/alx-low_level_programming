#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry
 * Return: Always 0
 */
int main(void)
{

	char low;

	for (low = 'a'; low <= 'z'; low++)
		putchar(low);
		purchar('\n');

	return (0);
}
